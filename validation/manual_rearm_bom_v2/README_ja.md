# バイパスコンデンサの実部品候補反映

C1〜C14にMurata GRM31C5C1H104JA01Lを選定候補として対応付けた。100 nF、初期±5%、50 V、C0G、1206。メーカーの2026-06-30版仕様は `schematics/power/manual_rearm_capacitors.json` に記録した。Lは包装コードで、2000個リールを購入する指示ではない。

既存の公称容量は変えていない。現在の比較電源上限3.393 V／5.25 Vは50 V定格内、比較温度−40〜125°Cは部品の−55〜125°C内。初期容量は95〜105 nFであり、100 nF以上を全条件で保証したとは扱わない。仕様p2の温度係数の適用範囲は25〜125°Cで、低温側はp6の別表があるため、一つの温度係数で全範囲を外挿しない。

C0Gを選ぶことで、高誘電率系の未確認DCバイアス低下を使った容量仮定への依存を避ける。ただし低周波測定の容量仕様は、実装配線を含む高周波インピーダンスや瞬時電源電圧の保証ではない。1206の配置・各IC直近の帰路・はんだ条件・基板曲げを次の机上設計で確認する。現在の回路に一律の実効容量合格値を新設して通過させることはしない。

43部品中42個に型番候補が付き、SW1の1個は未選定のまま。未統合のボタン・起動監視・外部停止・主電力経路は引き続き対象外。部品選定の進捗であり、通電許可や#6完了ではない。

再現：`python3 software/sim/circuits/build_manual_rearm_bom.py --out /tmp/manual-rearm-bom-v2-check`（未作成の出力先）。生成CSVとJSONを本ディレクトリと比較する。v1はコンデンサ未選定時点の履歴として保存する。
