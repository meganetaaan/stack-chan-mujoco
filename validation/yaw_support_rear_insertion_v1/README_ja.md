# 個別支持の後方挿入比較

左右各17部品の支持ユニットを後部板なしで挿入し、後から板を入れる公称連続掃引は通過。ただし支持を外装内で仮保持する工程が必要。後部板と支持を外で組むv2を優先候補とし、この記録を完成した作業手順とは扱わない。公差・工具・手・荷重保持は未確認。

再現は `check_yaw_support_insertion.py --out <未使用出力先>`。v2と異なり `--as-module` を指定しない。
