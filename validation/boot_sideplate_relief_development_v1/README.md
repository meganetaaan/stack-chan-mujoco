# ブーツ側板逃がしの検討：未合格

旧候補の接近点は左+0.34 rad、ロール座標でブーツ(-29,20.254607,26.702306)、フレーム(-29,19.4,26.4) mm。右は鏡像。上側横桟以外の側板端が残っていた。

v2では開口後端をx=-31.5 mmへ延長。左+0.34 radで距離0.976521804 mm、ブーツ(-8.5,20.316129,26.528387)、フレーム(-8.596345,19.4,26.204319) mmが接近した。基準1.1 mmに不合格。

v3では開口前端をx=3 mmへ延長した。左右とも単一の有効ソリッドで、z≤10 mmの領域は体積比較の数値精度内で維持。除去体積は約1744.128 mm³。外形拡大はないが、外観への開口の影響・剛性・端部丸みは未評価。

v3の連続区間評価は左−0.2975 rad、右+0.2975 radで距離1.1000000000000014 mmを検出した。基準1.1 mmに数値余裕0.00001 mmを加えた条件を満たさず、不合格として停止した。合格した一部区間だけから全範囲の成立は主張しない。

## 再現

engineering環境でLD_LIBRARY_PATHに.tools/root/usr/lib/x86_64-linux-gnuを設定し、各v*_cad/generator.pyを未作成の出力先に対し --out で実行する。次にsoftware/sim/structural/certify_native_yoke_gimbal.pyに --gimbal-dir validation/native_yoke_gimbal_wider_relief_v1/cad --moving-dir <生成先> --moving-part boot_shell --out <未作成の評価先> を渡す。判定はreport.jsonで確認する。

取付部の強度、全構成部品との干渉、製造承認は未完了。候補は採用しない。
