# v3再検査の失敗

頭/軸の板・支持への干渉は解消。しかし前側4座金の支持面が各0.085409 mm²欠けた。原因は隣接するケースねじ頭逃げ穴（半径2.5 mm）と座金（半径3 mm）の重なり。中心距離sqrt(5²+2²)=5.385 mmが半径和5.5 mmより小さい。面積基準を緩和せず不合格を保存。

再現：check_yaw_plate_seats.py --relocated --out <新規先>。次のv4は軸位置を変更した別形状。
