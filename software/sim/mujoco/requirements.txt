# Compatible version ranges, NOT a runtime-tested lockfile.
# Install into the same WSL virtual environment that already runs MuJoCo.
mujoco>=3.3.5,<4
stable-baselines3>=2.6,<3
gymnasium>=1.0,<2
numpy>=1.26,<3
tensorboard>=2.16,<3
