# 方角観測付き残差PPOの候補方策

70次元観測で131,072ステップ学習した。親は `r6_residual_seed20260921`。
**この候補の40試行受入は評価中であり、合格をまだ主張しない。**
外部基準による方角・横位置のノイズ付き観測を仮定する。IMU単独の姿勢推定ではない。
固定97000–97019、ランダム化98000–98019のシードを事前固定した。
初回方策の20/20・12/20はこの候補の成績として流用しない。

`initial_policy.zip` は親actor/criticを入力拡張した初期値。
`runtime.json`、`rng_sequence_audit.json` に乱数の適用順と親方策読み込みの影響を記録する。
再現には公開済み親方策と、`docs/R6_RL_ja.md` のコマンド全体を用いる。
